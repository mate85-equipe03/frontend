name: Frontend Actions
on: [push]
jobs:
  Lint:
    runs-on: ubuntu-latest
    steps:
      - name: Clonando código do repositório...
        uses: actions/checkout@v3
      - name: Rodando lint...
        run: npm install && npm run lint
